@{
    ViewData["Title"] = "Error";
}

<div class="text-center">
    <div class="alert alert-danger">
        <strong>Error!</strong> You did not select all values.
    </div>
    <button type="button" class="btn btn-secondary" onclick="location.href='@Url.Action("AddLesson", "Teacher", new { chosenTeacher = ViewData["chosen_teacher"], slot = ViewData["slot"] })'">Return</button><br><br>
</div>