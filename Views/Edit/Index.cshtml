@using SchoolPlanner.Models
@model Edit

@{
    ViewData["Title"] = "Edit";
}

@{ var classes = ViewData["classes"] as List<Class>;
    var classrooms = ViewData["classrooms"] as List<Classroom>;
    var teachers = ViewData["teachers"] as List<Teacher>;
    var subjects = ViewData["subjects"] as List<Subject>;}

<head>
    <title>Edit</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
</head>

<div class="text-center">
    <h1 class="display-4">Edit</h1>
</div>

<div class="container">
    <ul class="nav nav-pills">
        <li class="active"><a data-toggle="pill" href="#classrooms">Classrooms</a></li>
        <li><a data-toggle="pill" href="#classes">Classes</a></li>
        <li><a data-toggle="pill" href="#subjects">Subjects</a></li>
        <li><a data-toggle="pill" href="#teachers">Teachers</a></li>
    </ul>
  
    <div class="tab-content">
        <div id="classrooms" class="tab-pane fade in active">
            <ul class="nav nav-pills">
                <li class="active"><a data-toggle="pill" href="#add_classroom">Add</a></li>
                <li><a data-toggle="pill" href="#delete_classroom">Delete</a></li>
            </ul>
            <div class="tab-content">
                <div id="add_classroom" class="tab-pane fade in active">
                    <h3>Add a classroom:</h3>
                    @using (Html.BeginForm()) {
                        @Html.AntiForgeryToken()
                        @Html.ValidationSummary(true, "", new { @class = "text-danger"} )

                        <div class="form-group">
                            @Html.LabelFor(model=>model.ClassroomToAdd, "Number of the classroom:")
                            @Html.EditorFor(model=>model.ClassroomToAdd, new {htmlAttributes = new {@class="form-control"}})
                            @Html.ValidationMessageFor(model=>model.ClassroomToAdd, "", new {@class = "text-danger"})
                        </div>                    
                        <button type="submit" class="btn btn-primary">Add</button>
                    }
                </div>
                <div id="delete_classroom" class="tab-pane fade">
                    <h3>Delete the classroom:</h3>
                    @using (Html.BeginForm()) {
                        @Html.AntiForgeryToken()
                        @Html.ValidationSummary(true, "", new { @class = "text-danger"} )
                        if (classrooms != null && classrooms.Any()) {
                            @Html.DropDownListFor(model=>model.ClassroomToDelete, new SelectList(classrooms, "", "Number"), "Select the classroom to delete", new {htmlAttributes = new {@class="form-control"}})                   
                            <button type="submit" class="btn btn-primary">Delete</button>
                        }
                        else {
                            <p>No classrooms defined</p>
                        }
                    }
                </div>
            </div>
        </div>
        <div id="classes" class="tab-pane fade">
            <ul class="nav nav-pills">
                <li class="active"><a data-toggle="pill" href="#add_class">Add</a></li>
                <li><a data-toggle="pill" href="#delete_class">Delete</a></li>
            </ul>
            <div class="tab-content">
                <div id="add_class" class="tab-pane fade in active">
                    <h3>Add a class:</h3>
                    @using (Html.BeginForm()) {
                        @Html.AntiForgeryToken()
                        @Html.ValidationSummary(true, "", new { @class = "text-danger"} )

                        <div class="form-group">
                            @Html.LabelFor(model=>model.ClassroomToAdd, "Name of the class:")
                            @Html.EditorFor(model=>model.ClassToAdd, new {htmlAttributes = new {@class="form-control"}})
                            @Html.ValidationMessageFor(model=>model.ClassToAdd, "", new {@class = "text-danger"})
                        </div>                    
                        <button type="submit" class="btn btn-primary">Add</button>
                    }
                </div>
                <div id="delete_class" class="tab-pane fade">
                    <h3>Delete the class:</h3>
                    @using (Html.BeginForm()) {
                        @Html.AntiForgeryToken()
                        @Html.ValidationSummary(true, "", new { @class = "text-danger"} )
                        if (classes != null && classes.Any()) {
                            @Html.DropDownListFor(model=>model.ClassToDelete, new SelectList(classes, "", "Name"), "Select the class to delete", new {htmlAttributes = new {@class="form-control"}})                   
                            <button type="submit" class="btn btn-primary">Delete</button>
                        }
                        else {
                            <p>No classes defined</p>
                        }
                    }
                </div>
            </div>
        </div>
        <div id="subjects" class="tab-pane fade">
            <ul class="nav nav-pills">
                <li class="active"><a data-toggle="pill" href="#add_subject">Add</a></li>
                <li><a data-toggle="pill" href="#delete_subject">Delete</a></li>
            </ul>
            <div class="tab-content">
                <div id="add_subject" class="tab-pane fade in active">
                    <h3>Add a subject:</h3>
                    @using (Html.BeginForm()) {
                        @Html.AntiForgeryToken()
                        @Html.ValidationSummary(true, "", new { @class = "text-danger"} )

                        <div class="form-group">
                            @Html.LabelFor(model=>model.SubjectToAdd, "Name of the subject:")
                            @Html.EditorFor(model=>model.SubjectToAdd, new {htmlAttributes = new {@class="form-control"}})
                            @Html.ValidationMessageFor(model=>model.SubjectToAdd, "", new {@class = "text-danger"})
                        </div>                    
                        <button type="submit" class="btn btn-primary">Add</button>
                    }
                </div>
                <div id="delete_subject" class="tab-pane fade">
                    <h3>Delete the subject:</h3>
                    @using (Html.BeginForm()) {
                        @Html.AntiForgeryToken()
                        @Html.ValidationSummary(true, "", new { @class = "text-danger"} )
                        if (subjects != null && subjects.Any()) {
                            @Html.DropDownListFor(model=>model.SubjectToDelete, new SelectList(subjects, "", "Name"), "Select the subject to delete", new {htmlAttributes = new {@class="form-control"}})                   
                            <button type="submit" class="btn btn-primary">Delete</button>
                        }
                        else {
                            <p>No subject defined</p>
                        }
                    }
                </div>
            </div>
        </div>
        <div id="teachers" class="tab-pane fade">
            <ul class="nav nav-pills">
                <li class="active"><a data-toggle="pill" href="#add_teacher">Add</a></li>
                <li><a data-toggle="pill" href="#delete_teacher">Delete</a></li>
            </ul>
            <div class="tab-content">
                <div id="add_teacher" class="tab-pane fade in active">
                    <h3>Add a teacher:</h3>
                    @using (Html.BeginForm()) {
                        @Html.AntiForgeryToken()
                        @Html.ValidationSummary(true, "", new { @class = "text-danger"} )

                        <div class="form-group">
                            @Html.LabelFor(model=>model.TeacherToAdd, "Name of the subject:")
                            @Html.EditorFor(model=>model.TeacherToAdd, new {htmlAttributes = new {@class="form-control"}})
                            @Html.ValidationMessageFor(model=>model.TeacherToAdd, "", new {@class = "text-danger"})
                        </div>                    
                        <button type="submit" class="btn btn-primary">Add</button>
                    }
                </div>
                <div id="delete_teacher" class="tab-pane fade">
                    <h3>Delete the teacher:</h3>
                    @using (Html.BeginForm()) {
                        @Html.AntiForgeryToken()
                        @Html.ValidationSummary(true, "", new { @class = "text-danger"} )
                        if (teachers != null && teachers.Any()) {
                            @Html.DropDownListFor(model=>model.TeacherToDelete, new SelectList(teachers, "", "Surname"), "Select the teacher to delete", new {htmlAttributes = new {@class="form-control"}})                   
                            <button type="submit" class="btn btn-primary">Delete</button>
                        }
                        else {
                            <p>No teachers defined</p>
                        }
                    }
                </div>
            </div>
        </div>
    </div>
</div>